@model IEnumerable<Rent_All_Certificate.Models.Phase>

@{
    ViewBag.Title = "Index";
}

<h2>Pahse Management</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.PhaseName)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.PhaseName)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.PhaseID }) |
            @Html.ActionLink("Details", "Details", new { id=item.PhaseID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.PhaseID })
        </td>
    </tr>
}

</table>
